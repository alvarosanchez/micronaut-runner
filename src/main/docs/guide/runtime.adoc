A runner JAR is an ordinary executable JAR. Everything below is optional tuning and diagnostics.

[source,bash]
----
java -jar build/libs/my-app-0.1-all.jar
----

Arguments after the JAR are passed to your application untouched, and an exception thrown by your
application propagates exactly as it would from an ordinary main class: the JVM prints the stack trace and
exits with status 1. The launcher never wraps it.

The main method is chosen the way the `java` launcher chooses it, JEP 512 included: `main(String[])` is
preferred over `main()`, the whole hierarchy - superclasses and the interfaces they implement - is searched
for the first form before the second is considered, a `main` inherited as an interface default method
counts, and a non-static one is invoked on a new instance built from a non-private no-argument
constructor.

== System properties

[cols="30,15,55"]
|===
|Property |Default |Effect

|`micronaut.runner.mode`
|`run`
|`run` starts the application. `inspect` prints the index header and jar table, `list` prints the entries,
and `extract` writes an exploded copy. The non-`run` modes consume the remaining command line arguments.

|`micronaut.runner.timing`
|`false`
|Prints the time reached by each startup phase to standard error: archive opened, index read, class loader
ready, application entered. Use it to separate launcher cost from framework cost.

|`micronaut.runner.mmap`
|`true`
|Set to `false` to read the archive with positional file reads instead of a memory mapping. Slower, and
only needed on a platform or file system where mapping is unavailable or undesirable. Thread interrupts are
harmless in this mode: a read that an interrupt cuts short reopens the archive by its path and carries on.
If that path no longer names the file this JVM opened, class loading fails and asks for a restart, so
publish new versions under new paths as described in the deployment section below. Positional mode does not
make concurrent archive replacement safe.

|`micronaut.runner.verify`
|`false`
|Set to `true` to verify every entry's CRC-32 as it is read. Useful when you suspect a corrupted archive.

|`micronaut.runner.parent`
|`platform`
|The parent of the application class loader. `platform` isolates the application from anything on the real
`java.class.path`. `system` is an escape hatch for an agent or tool that must share types with the
application.
|===

== Exit codes

[cols="10,90"]
|===
|Code |Meaning

|`0` |Normal termination.
|`1` |The application threw. The JVM's default handler printed it.
|`2` |The launcher could not start: the archive could not be located, it was modified after packaging, or
its format version is not supported.
|===

== Archive lifetime and deployment

Treat a runner JAR as immutable from the moment its JVM opens it until that JVM terminates. Do not overwrite,
truncate, append to, sign, re-zip or otherwise modify a running archive. This requirement applies both to the
default memory-mapped mode and to `-Dmicronaut.runner.mmap=false` positional reads.

The format's length comparison happens against the file length captured when the archive opens. A nested
JAR's local-header signature is checked only on its first use, and a successful check is cached. These checks
are diagnostics for some stale packaging outputs, not ongoing file monitoring. CRC-32 verification with
`micronaut.runner.verify` can diagnose accidental byte corruption as complete entries are read; it is neither
authentication nor protection against concurrent mutation. The JDK leaves the visibility of changes to a
mapped file, and failures after truncation, dependent on the operating system and file system. Disabling mmap
can change the observed failure, but does not establish a supported live-mutation procedure.

Publish each version under a new path, preferably a versioned or content-addressed name. Verify the final
bytes, start a new JVM from that path, direct new work to it, and remove the old artifact only after the old
JVM has stopped. An atomic rename or symlink/pointer update may select the new path for _new_ launches without
changing the file already open by an existing POSIX process. It does not update that process, which must keep
using its original immutable file. On Windows, an open mapping commonly prevents deleting or replacing its
file, so distinct versioned filenames are required. If deployment requires reusing one filename, stop every
JVM using it before replacement and start them again afterwards.

The launcher deliberately keeps the archive open after the application `main` method returns because
background threads may still load classes. Returning from `main` is therefore not a replacement boundary.

== Running from an exploded layout

`extract` writes the archive out as a directory: the dependencies as real JAR files under `lib/`, and the
application classes in a JAR whose manifest names your main class and lists those dependencies in its
`Class-Path`.

[source,bash]
----
java -Dmicronaut.runner.mode=extract -jar my-app-0.1-all.jar --destination ./app
java -jar ./app/my-app.jar
----

The extracted form runs entirely on the JDK's own application class loader, with no custom class loader
anywhere. That matters for the JDK's ahead-of-time cache: as described in <<cdsAndAot>>, classes loaded by
a user-defined class loader cannot be AOT-linked, so an application that will be started thousands of
times in the same container image may be better served by extracting once at image build time.

=== Publication and replacement

Extraction stages the complete layout in a temporary sibling directory before publishing it. Without
`--force`, the destination is checked again after staging and the final move does not replace an existing
path. If another process populates the destination while extraction is running, extraction fails and
preserves that occupant.

With `--force`, existing output is renamed to a temporary sibling backup before the staged layout is moved
into place. A publication failure restores the backup when possible. If restoration or cleanup also fails,
the error names the backup (or the part of it cleanup could not remove). A failed restoration therefore
leaves the old output recoverable instead of silently losing it. Successful replacement removes the backup.

Forced backup, publication, and restoration first request an atomic filesystem move and fall back to an
ordinary same-filesystem move only when atomic moves are unsupported. Java's portable filesystem API does
not provide an atomic directory exchange or bind earlier path-identity checks to a later move. Consequently,
publishing over an existing empty destination or performing a forced replacement can briefly leave the
destination absent. Forced replacement is rollback-protected but not universally transactional, and a
process able to rewrite destination path components can still race publication. Keep the source and
destination parent writable only by trusted participants.
