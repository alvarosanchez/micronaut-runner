package com.example.metadata;

/** A class in a package without a named manifest section, used to read the main attributes at runtime. */
public final class MainManifestMetadata {

    private MainManifestMetadata() {
    }
}