dependencyResolutionManagement {
    repositories {
        maven {
            url = uri(providers.gradleProperty('runner.repo').get())
            metadataSources { gradleMetadata() }
        }
    }
}

rootProject.name = 'published-module-metadata-consumer'
